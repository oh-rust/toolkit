[workspace]
resolver="1"
members = [
    "oh-str-unbox",
]

[profile.release]
lto = true          # Link Time Optimization
codegen-units = 1   # 减少生成单元，提高优化空间
opt-level = "z"     # "z" 优化体积，"s" 优化大小，默认 "3" 优化速度
strip = true        # 自动剥离符号表 (无需手动执行 strip 命令)